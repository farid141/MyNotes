# Overview

Ada 2 versi dotnet yang digunakan:

- .net frameworks: windows-platform (lama)
- .net core: cross-platform (baru) bisa dijalankan di docker

strongly-typed
statically-compiled

Great Microsoft based IDE for (refactoring, tooling, intellisense)

Great new features:

1. records:

    - value-based, bisa komparasi object langsung
    - merupakan simple + immutable data structures
    - with-keyword untuk cloning dan modifying
    - object destructuring

2. pattern matching
3. linq ORM?
