# Database

Terdapat konsep database dalam redis, ini berguna untuk mengisolasi database semisal untuk projek lain.

Jumlah database maksimal dapat ditentukan pada konfigurasi redis

Nama database hanya bisa menggunakan angka, secara default client akan menggunakan `database 0`. Untuk berpindah database bisa menggunakan `select db_name`
