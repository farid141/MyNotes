# Gitignore

Mengabaikan file agar tidak ikut tercatat dalam commit

## Add file to gitignore

- Jika file sudah digunakan, hapus cache `git rm --cached filename.txt`
- Tambahkan file/folder ke `.gitignore`
