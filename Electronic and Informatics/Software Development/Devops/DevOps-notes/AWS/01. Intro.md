# Intro

AWS merupakan platform cloud berbayar walopun ada versi gratis 12 bln.

kenapa make sure shutdown t2 small instance kalo sedang tidak dipakai

dalam ec2 instance amazon ketika direstart apa public IP berubah?

EC2 instance kita bisa mengecek disk size lewat aws maupun ssh ke instance langsung
