# Penjelasan

Pada sistem operasi linux, kita dapat mengetahui detail dari kegunaan commands pada man page. Biasanya format halaman manual sebuah command memiliki struktur yang sama.

## Options

-a=[] berarti dapat digunakan beberapa kali dalam satu command
docker run -a stdin -a stdout -i -t ubuntu /bin/bash
